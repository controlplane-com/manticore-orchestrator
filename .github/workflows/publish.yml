name: Publish Docker Image

on:
  push:
    branches: [master]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute next tag
        id: version
        run: |
          LATEST=$(git tag --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -1)
          if [ -z "$LATEST" ]; then
            LATEST="v5.2.1"
          fi
          MAJOR=$(echo $LATEST | cut -d. -f1 | tr -d 'v')
          MINOR=$(echo $LATEST | cut -d. -f2)
          PATCH=$(echo $LATEST | cut -d. -f3)
          NEW_TAG="v${MAJOR}.${MINOR}.$((PATCH + 1))"
          echo "tag=$NEW_TAG" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/api/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/controlplane-com/manticore-orchestrator/manticore-cpln-api:${{ steps.version.outputs.tag }}
          build-args: IMAGE_VERSION=${{ steps.version.outputs.tag }}

      - name: Build and push agent image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/agent/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/controlplane-com/manticore-orchestrator/manticore-cpln-agent:${{ steps.version.outputs.tag }}
          build-args: IMAGE_VERSION=${{ steps.version.outputs.tag }}

      - name: Build and push UI image
        uses: docker/build-push-action@v6
        with:
          context: ./ui
          file: build/ui/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/controlplane-com/manticore-orchestrator/manticore-cpln-ui:${{ steps.version.outputs.tag }}
          build-args: IMAGE_VERSION=${{ steps.version.outputs.tag }}

      - name: Build and push backup image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/backup/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/controlplane-com/manticore-orchestrator/manticore-cpln-backup:${{ steps.version.outputs.tag }}
          build-args: IMAGE_VERSION=${{ steps.version.outputs.tag }}

      - name: Create and push git tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ steps.version.outputs.tag }}
          git push origin ${{ steps.version.outputs.tag }}
